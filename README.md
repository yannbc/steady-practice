# steady-practice — `gh-pages` branch

This branch is **generated output**, not source. It holds the production
build that GitHub Pages serves.

Do not edit anything here by hand — changes will be overwritten by the
next build.

The source lives on [`main`](https://github.com/yannbc/steady-practice/tree/main).
To update the live site, rebuild with `npm run build` on `main` and
publish the contents of `dist/` to this branch.
